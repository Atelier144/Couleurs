<%= react_component("Twitter", {
    mode: @mode,
    provider: @provider,
    uid: @uid,
    name: @name,
    image: @image,
    description: @description,
    url: @url,
    twitter_url: @twitter_url
})%>
